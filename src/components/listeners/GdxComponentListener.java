package components.listeners;

import general.interfaces.GdxListener;

/**
 * Markup interface for a listener passing on events occuring
 * within a component, and therefore specified by a component.
 *
 */
public interface GdxComponentListener extends GdxListener { }